# This is an EXAMPLE — do NOT commit real secrets to Git.
# Create the actual secret in your cluster with:
#
#   kubectl create secret generic journal-api-secrets \
#     --from-literal=DATABASE_URL='postgresql://user:pass@host:5432/career_journal?sslmode=require' \
#     --from-literal=OPENAI_API_KEY='your-key' \
#     --from-literal=OPENAI_BASE_URL='https://models.inference.ai.azure.com' \
#     --from-literal=OPENAI_MODEL='gpt-4o-mini'
#
apiVersion: v1
kind: Secret
metadata:
  name: journal-api-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgresql://pgadmin:CHANGE_ME@your-pg-server.postgres.database.azure.com:5432/career_journal?sslmode=require"
  OPENAI_API_KEY: "your-openai-api-key"
  OPENAI_BASE_URL: "https://models.inference.ai.azure.com"
  OPENAI_MODEL: "gpt-4o-mini"
